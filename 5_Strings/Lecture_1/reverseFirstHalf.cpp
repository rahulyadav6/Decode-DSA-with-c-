#include <iostream>
#include <algorithm>
using namespace std;
int main(){
    // string s;
    // getline(cin,s);
    // int n = s.size();
    // reverse(s.begin()+0,s.begin()+(n/2));
    // cout<<s;







    //substring
    string s = "abcdefg";
    cout<<s.substr(1,3);

}